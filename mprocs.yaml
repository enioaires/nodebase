procs:
  database:
    shell: docker compose up
    stop: SIGTERM
    autostart: true

  next:
    shell: npm run dev
    stop: SIGTERM
    autostart: true

  inngest:
    shell: npm run inngest:dev
    stop: SIGTERM
    autostart: true
